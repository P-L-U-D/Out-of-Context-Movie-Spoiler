import React, { Component } from 'react';

class Highlights extends Component {
   render() {
      return(
         <div className="highlights">
            <h2>BEAUTY!</h2>
            {/* return(
               {this.props.savedResults}
            ) */}
            {/* <div id={this.props.dbKey}></div>
            <button onClick={() => this.props.removeSubmission}>Read</button> */}
         </div>
      )
   }
}

export default Highlights